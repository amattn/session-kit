Happy to help narrow that down. To suggest good names I need a bit more context:

```
1. What does the module do?
   A. Data validation / sanitization
   B. Authentication / authorization
   C. Database access / persistence
   D. HTTP client / external API calls
   E. Background jobs / task queue
   F. Something else — describe briefly

2. What's the naming style in the rest of the codebase?
   A. Short and generic (e.g., utils, helpers, core)
   B. Domain-specific nouns (e.g., orders, shipments, catalog)
   C. Action-oriented (e.g., sync, ingest, export)
   D. Not established yet — open to anything
```

Once I know 1 and 2, I'll come back with 3-4 concrete name candidates to pick from.
