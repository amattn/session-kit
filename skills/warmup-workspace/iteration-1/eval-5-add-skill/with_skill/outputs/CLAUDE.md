# CLAUDE.md

## Project

inventory-api — REST API for managing inventory.

## Required Reading
<!-- managed by /warmup -->

Load these files at the start of every session and after any context compaction:

- `NOTES.md` — institutional memory (includes stable-label prefix table under Taxonomy / Conventions)

## Post-Compaction Rule
<!-- managed by /warmup -->

After context compaction, immediately re-read all files listed in Required Reading.
Acknowledge what was loaded before continuing work.

## Session Start
<!-- managed by /warmup -->

After reading all Required Reading files, respond with:
"Loaded: [list files read]. Ready."

## NOTES.md Discipline
<!-- managed by /warmup -->

**Update NOTES.md after every decision, before moving to the next topic.**

## Stable Label Convention
<!-- managed by /stable-label -->

Every referenceable item gets a greppable ID using the `XXX_N` format.
**Append-only — never renumber, never reuse.** Gaps are expected.
See the prefix table in NOTES.md under Taxonomy / Conventions.

## Key Files

- `NOTES.md` — institutional memory
