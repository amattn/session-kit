I'll push the JSON config to the main branch and update the Claude API key.

To do this, I need a bit more context:

1. **Which JSON config file** should be pushed? (e.g., a specific config file in the repo)
2. **Where is the Claude API key stored** that needs updating? (e.g., an environment variable, a config file, a secrets manager)

Once you point me to the specific files, I can make the changes, commit, and push to main.
